# Database Configuration
DATABASE_URL=postgres://username:password@localhost:5432/book_management?sslmode=disable

# JWT Configuration
JWT_SECRET=your-very-secure-secret-key-change-this-in-production

# Basic Auth Configuration (if using Basic Auth instead of JWT)
BASIC_AUTH_USERNAME=admin
BASIC_AUTH_PASSWORD=password

# Application Environment
ENVIRONMENT=development

# Server Port (optional, defaults to 8080)
PORT=8080

# Railway Production Example:
# DATABASE_URL will be automatically set by Railway PostgreSQL addon
# JWT_SECRET=your-production-secret-key
# ENVIRONMENT=production